# Aurtiro Website

Vite + React + TypeScript + Tailwind implementation for https://www.aurtiro.com.

## Local development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The `public/CNAME` file is included so production builds preserve the custom domain.
